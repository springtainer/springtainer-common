name: review_internal

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: setup-java
        uses: actions/setup-java@v1
        with:
          java-version: 11

      - name: checkout
        uses: actions/checkout@v1

      - name: caching
        uses: actions/cache@v1
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: compile
        run: mvn clean compile test-compile

      - name: unit-tests
        run: mvn surefire:test

      - name: integration-tests
        run: mvn jacoco:restore-instrumented-classes failsafe:integration-test failsafe:verify

      - name: sonar-analyse
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          export GITHUB_PULL_REQUEST=$(cut -d / -f 3 <(echo ${GITHUB_REF}))
          echo "sonar.pullrequest.provider=Github" > sonar.properties
          echo "sonar.pullrequest.key=${GITHUB_PULL_REQUEST}" >> sonar.properties
          echo "sonar.alm.github.app.privateKey.secured=${GITHUB_TOKEN}" >> sonar.properties
          echo "sonar.alm.github.app.id=x-access-token" >> sonar.properties
          echo "sonar.pullrequest.branch=${GITHUB_REF}" >> sonar.properties
          echo "sonar.pullrequest.github.repository=${GITHUB_REPOSITORY}" >> sonar.properties
          mvn sonar:sonar \
            -Dsonar.host.url=http://localhost:8091/ \
            -Dsonar.java.binaries=./target/classes -X

      - name: pom-analyse
        run: mvn enforcer:enforce
